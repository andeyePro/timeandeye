# andeye brand

`andeye.svg` is the canonical andeye mark (one closed stroked path, 365×235); `andeye-logo.afdesign` is its Affinity Designer source.
`Sources/timeandeyeCore/AndeyeLogo.swift` hardcodes the SVG's path data verbatim — if the mark changes, update both.
