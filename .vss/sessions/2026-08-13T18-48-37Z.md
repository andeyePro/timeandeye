# /vsss session — start 2026-08-13T18:48:37Z – budget 5h — --sessions 3

## Initial plan

- **Args (verbatim):** Work TODO.md "Martin's 13 Aug replies" top to bottom:
  first the correction-overlearning fix set per
  docs/superpowers/specs/2026-08-13-correction-overlearning-diagnosis.md
  (fixes 1-5; build 6 too but flag it in fromClaude), then the correction
  ledger, then the rest. Mac-verify everything via bash .vibe/mac-test.sh
  (baseline 933/0); build the ImageRenderer snapshot harness BEFORE any
  Settings/UI layout work so formatting is verified visually, then redo the
  Billing/currency pane with it; push every green build to main, watch CI;
  poll Time&I-fromMartin each iteration. Fable budget is granted this week -
  don't spare the horses.
- **Flags:** --sessions 3 (marker armed, remaining=2). No --hours → 5h/window.
- **Push policy:** autonomous push to main PRE-AUTHORISED by the invocation
  args ("push every green build to main, watch CI") — overrides the /vsss
  local-only default for this run. CI watched after every push.
- **SSH scope:** the invocation args pre-authorise ONE SSH route — the
  established Mac build bridge (bash .vibe/mac-test.sh →
  ambitick@host.docker.internal), used for build/suite verification and for
  pulling back snapshot PNGs the suite renders. Any other SSH remains
  hard-escalate.
- **Fable rung:** pre-authorised by args ("Fable budget is granted this
  week - don't spare the horses") — recorded per the --fable-subagents
  convention; mechanical roles still route to cheaper rungs.
- **fromto channels:** ACTIVE (existing Time&I-fromMartin.md /
  Time&I-fromClaude.md / Time&I-Q&A-archive.md in /brain2/andeye/).
  fromClaude current generation (recorded 2026-08-13 pre-session): 1
  feel-test two Time windows; 2 feel-test review walk-through; 3 queue
  extras a-d (after 2); 4 ANSWER end-time orphan auto-apply (default-go);
  5 connections tiers sign-off. Next thread number: 6.
- **Priority queue** (from args + TODO "Martin's 13 Aug replies" section):
  1. Correction-overlearning fix set (spec fixes 1-6; 6 flagged to Martin
     when built) — Core trio (2 specificity, 5 surface-key normalisation,
     6 symmetric discount) then Mac pair (1 duration-gated teaches,
     3 forget-refiles) then card fix (4).
  2. Correction ledger (TODO #1 sub-parts a+b) on the spec's inventory.
  3. ImageRenderer snapshot harness (before ANY Settings/UI layout work).
  4. Billing/currency pane redo (snapshot-verified).
  5. Remaining 13 Aug programme items top to bottom (reassign display,
     away rescue more-auto + end-time orphans, double-forget escalation,
     correction-propagation UI, billable visibility + redo, mis-filed
     notice, then the rest of TODO).
- **Baselines:** Mac suite via bridge TOTAL 933/0 (verified 2026-08-13
  evening); Linux subset 787/0; CI green through 0189785.
- **Stale-TODO sweep (feedback_vsss_todo_staleness):** the 13 Aug section
  is hours old (fresh). Older open items checked this evening — none
  superseded by the new programme except "Reassign scope" (cross-referenced,
  kept) and away-mode (c) (updated to unblocked). No stale picks risk.

## Iterations
## Iter 1 — over-learning fix set, Core trio (fixes 2/5/6)

- **Plan:** Mode B (args). Diagnosis spec fixes 2 (LearningStore
  specificity blend + prior scaling), 5 (Surface title-key normalisation
  + primed.json one-shot migration wired at AppController load), 6
  (rankedDisplaced also fires on a displaced .primedSurface; pins/sticky/
  human word still exempt). Fix 2's first formulation (bare s× on the
  log-ratio) was caught wrong-direction in design review — negative
  log-likelihoods get STRONGER when scaled down — replaced with a blend
  toward the unmatched constant before any code ran.
- **Files touched:** Sources/timeandeyeCore/{LearningStore,Models,
  Attributor}.swift, Sources/timeandeyeMac/AppController.swift,
  Sources/timeandeyeChecks/{ModelsChecks,AttributionLearningChecks}.swift,
  CHANGELOG.md, TODO.md.
- **Checks:** +4 (L8 specificity; fix-6 operator byte-pin incl.
  pin-never-discounts sanity; surface-normalisation cases; migration
  cases). Linux subset 791/0; Mac bridge suite 937/0.
- **Commit:** 1140c57, pushed (pre-authorised by args); CI watch armed.
- **Outcome:** success.
- **Notes:** fix 6 flagged to Martin as fromClaude item 6
  (FYI-unless-you-object). fromMartin polled at iter start: empty.
## Iter 2 — over-learning fixes 1 + 4

- **Plan:** optimiser refined (continue fix set). Fix 1: TeachScope in
  Core (bulkReassignTeaches floor 120s in multi-session selections;
  reviewTeachWeight skips sub-floor surfaces in multi-surface sweeps,
  duration-scales the rest, full +2 from 300s; single-target gestures
  unchanged); Attributor.assign gains weight:; assignReview + 
  teachAssociation + reassignTimelineSessions wired; calendar teach
  follows taught surfaces only. Fix 4: TimelineView recorded line shows
  ONE decision whole (span triple when span provenance exists, else
  session triple) — never slice certainty + span provenance.
- **Files:** TeachScope.swift (new), Models.swift
  (teachingSignalsWithDurations), Attributor.swift, AppController.swift,
  TimelineView.swift, TeachScopeChecks.swift (new, registered),
  CHANGELOG.md, TODO.md.
- **Checks:** +4 TeachScope. Linux 795/0, Mac 941/0.
- **Commits:** 3978ff6 (fix 1), e394069 (fix 4), pushed; CI watch armed.
- **Outcome:** success.
- **Notes:** fromMartin polled at iter start: empty. Walk-through confirm,
  AI paste and drawer sweep all route via assignReview, so fix 1 covers
  all three bulk paths with one seam.
## Iter 3 — over-learning fix 3 (forget targets the recorded decider)

- **Plan:** optimiser's iter-1 refined args still govern (they scheduled
  fixes 1/3/4 explicitly; no fresh optimiser call needed until that
  scope completes). Fix 3: Attributor.forgettable gains a recorded-
  provenance overload mapping the RECORDED decider to its store while it
  still holds something (ranked → suppress recorded target's counts;
  primed/email/site/sticky → that store); falls back to today's ladder
  for human-word records and drained stores. EvidenceCardView passes
  recorded provenance+target; popover live card unchanged. Slice
  re-derivation rides the existing post-forget refile pass (persist
  already schedules it; refileMode governs) — no new lane logic.
- **Files:** LearningStore.swift (pullsToward), Attributor.swift,
  AppController.swift (overload), EvidenceCardView.swift,
  AttributionLearningChecks.swift, CHANGELOG.md, TODO.md.
- **Checks:** +1 (forgettable recorded-targeting, 4 branches).
  Linux 796/0, Mac 942/0.
- **Commit:** 8aafca5, pushed; CI watch armed.
- **Outcome:** success. Whole 6-part fix set now landed.
- **Notes:** fromMartin polled: empty. Next per refined args: correction
  ledger (a)+(b), then tab-change latency (e), then ImageRenderer
  snapshot harness, then Billing pane redo.
## Iter 4 — correction ledger (a)+(b)

- **Plan:** CorrectionLedger (Core): Record {at, gesture verb, app/title/
  url, target, weight, displaced, isForget}; cap 2000 / 180d; lastTeach
  (exact surface beats app-level). Attributor records in confirm/assign/
  learnSurface/forget (forget split into forgetApply so
  forgettableWithout previews cannot write history). Gesture verbs wired
  through every AppController call site per the diagnosis inventory.
  Persisted corrections.json (load+prune at init, save in
  persistAssociations). Card names the correction behind remembered/
  ranked pulls ("↳ taught … — you reassigned the slice on …");
  RulesLedgerView gains a searchable read-only Corrections segment.
  Manual (site + MANUAL.md) updated same commit.
- **Checks:** +4 (gesture/weight/displacement recording incl. the
  sticky-vs-prime displacement subtlety; preview purity; lastTeach
  precedence incl. forget-exclusion; cap/prune/Codable).
  Linux 800/0, Mac 946/0.
- **Commit:** 4555373, pushed; CI watch armed.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty. Deferred with a TODO note: the
  "what has each correction since decided" reverse index (needs
  provenance to carry a correction id — primed-store schema change,
  design first). Next per refined args: tab-change latency (e), then the
  ImageRenderer snapshot harness, then the Billing pane redo.
## Iter 5 — snapshot harness (agent eyes) + Billing pane redo

- **Plan:** swapped ahead of latency (e) deliberately: the harness is
  self-contained while (e) touches SessionTracker's hottest logic —
  better taken with fresh context; the args only demanded harness-before-
  UI-layout-work, which is honoured (Billing redo used it same iter).
- **Harness:** timeandeyeSnapshots exec + SnapshotHarness (timeandeyeUI).
  EMPIRICAL FINDINGS: ImageRenderer works headless over SSH for pure
  SwiftUI but placeholder-glyphs AppKit-backed containers (List/
  NavigationSplitView/Form) — switched to offscreen NSHostingView +
  cacheDisplay, which renders everything and needs no window session.
  Route: bash .vibe/mac-test.sh run timeandeyeSnapshots /tmp/andeye-snaps
  → scp PNGs → Read. SettingsView gained initialCategory (pane-
  addressable). Also spotted in the popover snapshot: the brand mark
  renders as a missing-image glyph in the harness context (bundle
  resource?) — cosmetic, harness-only, noted not chased.
- **Billing redo (snapshot-verified before/after):** BOTH of Martin's
  complaints reproduced exactly in the before-PNG (3-line "Curren-cy
  symbol" label; caption scrambled by the wrapping inline-link HStack).
  After: Billing section (renamed from Currency), one-line LabeledContent
  + 56pt field, one-sentence caption, new Billable items list
  (AppController.billableItems: billable projects + explicit task
  overrides), link sentence on its own row. Manual synced.
- **Checks:** Linux 800/0, Mac 946/0. **Commit:** cd5ea73 pushed; CI
  watch armed.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty. Remaining from the programme:
  latency (e), Donut-section question, mis-filed notice relocation +
  highlight, reassign-scope display build, away rescue build,
  double-forget escalation, propagation UI, billable badges + redo.
## Iter 6 — tab-change latency (e): event-driven early poll

- **Plan:** display was already instant once the tracker sees a change —
  the pause is the 2 s sensor cadence. SensorHub gains an event layer:
  NSWorkspace didActivateApplication → early poll + AX observer re-home;
  AXObserver on the front app (kAXFocusedWindowChanged, re-pointing a
  per-window kAXTitleChanged subscription — a Chrome tab switch IS a
  title change). Events trailing-coalesce (0.25 s) with ≥0.5 s spacing so
  page-load title churn can't hammer the AppleScript URL fetch; the 2 s
  timer stays the backstop; C10 main-runloop guard honoured; AX failure
  degrades to exactly the old behaviour.
- **Checks:** Mac 946/0 (compile + suite; no off-device behavioural
  coverage possible — flagged as fromClaude feel-test 7 with an explicit
  beachball watch).
- **Commit:** 25f92c3, pushed; CI watch armed.
- **Outcome:** success. All five of the args' named workstreams (fix set,
  ledger, latency, snapshot harness, Billing pane) are now landed.
- **Notes:** fromMartin polled: empty. Remaining programme items:
  reassign-scope display build, away rescue more-auto build, double-
  forget escalation, propagation UI, billable badges + ⌘⇧Z redo + notice
  z-order, mis-filed notice relocation/highlight, Donut-section question.
## Iter 7 — reassign scope, both halves (optimiser order item 1)

- **Plan:** optimiser (post-iter-6) refined args with a 7-item order;
  item 1 built: relabelCurrentSession cuts at the current tab/window's
  contiguous same-surface stretch (currentSurfaceStretchStart walks
  spans back while the surface matches) — pre-stretch time flushes to
  the task it stood as via the commitPendingSwitch pattern; single-
  surface sessions degrade to the old whole-session relabel, so every
  existing check (incl. same-surface 724 and pinned pick-comment-pick)
  passed unchanged. Display: currentSurfaceElapsed +
  AppController.reassignScopeCaption ("1m on this tab · 10m total",
  nil when within a minute) shown under the popover's Reassign mode
  label as "moves …" — menu bar untouched. Manual synced.
- **Checks:** +1 scoped-relabel scenario. Linux 801/0, Mac 947/0.
- **Commit:** b0d59ea, pushed; CI watch armed.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty. Next per optimiser order:
  (2) mis-filed notice relocation + timeline highlight + Donut-section
  settle, (3) double-forget escalation, (4) billable badges,
  (5) redo/undo core, (6) away rescue, (7) propagation UI.
## Iter 8 — mis-filed posted marking + Review cross-link + Donut settle (optimiser item 2)

- **Plan:** contradiction pass publishes flagged sessions
  (contradictedPostedFindings id→Finding); timeline slice renderer adds
  an orange outline + ⚠ + tooltip naming what today's rules would say;
  Review window gains the cross-linked notice with the jump; Settings
  link now says "(marked ⚠)". Donut-section question settled as a
  feel-test wording error (no such section ever existed) — recorded in
  TODO/CHANGELOG, no build.
- **Checks:** Linux 801/0, Mac 947/0 (UI-only; snapshot harness covers
  Settings/Review renders — timeline marking needs real flagged data, so
  it rides Martin's next real-data look).
- **Commit:** ed91d7f, pushed; CI watch armed.
- **Outcome:** success. Optimiser items 1-2 done; next (3) double-forget
  escalation, (4) billable badges, (5) redo/undo core, (6) away rescue,
  (7) propagation UI.
- **Notes:** fromMartin polled: empty.
## Iter 9 — double-forget escalation (optimiser item 3)

- **Plan:** EvidenceCardView tracks forgets-per-card-presentation; the
  second unlocks "✕ still wrong? delete ALL learned experience for
  <task>" → AppController.forgetAllExperience (snapshot-undoable) →
  Attributor.forgetAllExperience (LearningStore.eraseAllExperience wipes
  every count + the total for that target; primes to it dropped; ledger
  gesture forgetAll/isForget). Pins/stickies/rules stay by design.
- **Design note surfaced by the check:** wiping a target legitimately
  RAISES sharers' scores (shared-feature specificity recovers to 1.0) —
  asserted as never-falls, not score-identical; totals untouched so the
  old ratio-inflation hazard stays impossible.
- **Checks:** +1 wipe property (4 branches + ledger honesty).
  Linux 802/0, Mac 948/0.
- **Commit:** 8295843, pushed; CI watch armed.
- **Outcome:** success. Next: (4) billable badges, (5) redo/undo core,
  (6) away rescue, (7) propagation UI.
- **Notes:** fromMartin polled: empty.
## Iter 10 — billable badges (optimiser item 4)

- **Plan:** currency-symbol mark bottom-left on effectively-billable
  timeline slices (comment-bubble corner grammar, slivers skip); donut
  legend TASK rows gain the billable capsule (incl. inherited — the
  "set Ambi4 billable and saw nothing" case; project rows already had
  one). AppController.currencySymbolText single source. Manual synced.
- **Checks:** Linux 802/0, Mac 948/0.
- **Commits:** be829e4 + 957a137 (TODO note followed one commit late —
  the TODO edit's anchor missed and the push had gone; convention breach
  noted, fixed immediately). CI green both.
- **Outcome:** success. Reply-12 remainder = ⌘⇧Z redo + notice z-order,
  which IS optimiser item 5 (undo/redo core) — next.
- **Notes:** fromMartin polled: empty.
## Iter 11 — undo/redo core, slice 1 (optimiser item 5)

- **Plan:** spec written first (2026-08-13-undo-redo-core.md — the
  NSUndoManager-discipline design, replay-stability conversion rule, 5
  conversion batches). Core: UndoStack redo stack + mode routing
  (inverse-run registrations → redo entry; replay-run → undo entry;
  explicit id-stable redo: closures; legacy steps = honest boundaries;
  fresh edits clear redo; legacy pop() clears redo). Controller: redo()
  on the same serial chain, ⌘⇧Z on the key monitor, banners now say
  "undid/redid — <label>", banner level raised above the popover
  (.statusBar → popUpMenu+1 — reply 12's hidden notice). Manual +
  keyboard page updated.
- **Check iteration note:** first draft of the self-re-registration
  check used one-shot fake closures and failed by construction; rewritten
  as a genuine self-re-registering site (setValue ping-pong) — machinery
  was right, the fake was wrong.
- **Checks:** +3 UndoStack. Linux 805/0, Mac 951/0.
- **Commit:** 9c78447, pushed; CI watch armed.
- **Outcome:** success. Remaining spec batches: 2 live pick/Stop, 3
  billable marks redo closures, 4 forget family, 5 review/timeline audit.
- **Notes:** fromMartin polled: empty.
## Iter 12 — live pick + Stop join ⌘Z (spec batch 2)

- **Plan:** userPicked registers "switch to <task>" (undo: attributor +
  calendar snapshots restore, relabel back to the prior running task or
  stop if the pick had started tracking; redo: re-pick — self-
  re-registering). userStopped registers "stop tracking <task>" (undo:
  resume — the stop-to-undo gap stays untracked; redo: stop again).
- **Checks:** Mac 951/0 (tracker paths covered by existing suites; the
  registration glue is Mac-side).
- **Commit:** 0fbfbef, pushed; CI green.
- **Outcome:** success. Spec batches 3-5 (billable/forget/review
  conversions) remain; optimiser items 6 (away rescue) + 7 (propagation
  UI) remain.
- **Notes:** fromMartin polled: empty.
## Iter 13 — redo conversions: billable + forget (spec batches 3-4)

- **Plan:** id-stable replays added to setSessionBillable (id-keyed flag
  write), setProjectBillable, setTaskBillable, forget, and
  forgetAllExperience. Batch 5 (review/timeline) deliberately awaits its
  id-stability audit — recreation-based inverses stay boundaries.
- **Checks:** Mac 951/0. **Commit:** 8f8af4f, pushed; CI green.
- **Outcome:** success. Optimiser queue remaining: (6) away rescue
  more-auto, (7) correction-propagation UI. Undo notice z-order +
  redo + transparency now all live — reply 12 fully closed bar batch 5.
- **Notes:** fromMartin polled: empty (checked at iter 12).
## Iter 14 — away rescue, Core half (optimiser item 6, part 1)

- **Plan:** AwayRescue.plan (pure Core): attribute-per-span via injected
  engine closure, merge same-target runs across ≤120s gaps, 60s floor,
  window clipping, away-rows-only filter; low certainty proposes (red,
  queues on apply), doNotTrack/unattributable + unbridged gaps stay
  pinned (keptSeconds). One check expectation corrected during TDD (a
  merged run bridges its internal gap — kept counts only unbridged
  time).
- **Checks:** +4. Linux 809/0, Mac 955/0.
- **Commit:** 33e9046, pushed; CI green.
- **Outcome:** success. Mac half (Settings preview/apply through the
  split/reassign machinery + end-time-orphan auto-apply) next — the
  end-time half shares the same attribute-orphans mechanism.
- **Notes:** fromMartin polled at iter 13: empty.
## Iter 15 — away rescue Mac half + UI-compile-gate incident (item 6 done)

- **Built:** Settings ▸ Away rescue (scan stretches — contiguous
  observedWhileAway evidence, 10-min gap split; Preview = engine rebuild
  with red sub-bar certainties + kept time; apply carves proposals from
  overlapping UNPUSHED sessions at engine certainty/provenance via
  replaceSession(teaching: false) — L2 held, posted untouched, one ⌘Z).
  Snapshot-verified (settings-maintenance.png). Manual synced (settings
  page + MANUAL popover section). TODO's away item now tracks only the
  end-time-orphan extension.
- **INCIDENT (honest record):** the check suite does NOT build
  timeandeyeUI, so iter 9's dangling @ViewBuilder in EvidenceCardView
  shipped green through 4 commits (8295843..33e9046 — make-app would
  have failed for Martin), plus a type-check-ceiling break in
  TimelineView's tooltip concat. Both caught by the snapshot build this
  iter and fixed; CHANGELOG carries the correction naming the affected
  commits. Mitigations: CLAUDE.md documents the UI gate; the CI step
  needs workflow scope the PAT lacks → fromClaude item 8 (exact lines on
  Martin's clipboard). Transient bridge SSH hang also hit one run
  (killed, retried clean); a push race false-alarm resolved on retry —
  real blocker was the workflow-scope rejection.
- **Checks:** Mac suite 955/0 + snapshots build clean (the real UI gate).
- **Commit:** 669a19b, pushed; CI watch armed.
- **Outcome:** success with incident recorded.
- **Notes:** fromMartin polled: empty.
## Iter 16 — correction propagation picker (optimiser item 7, v1)

- **Built:** PropagationOffer plumbing (immediate contradiction pass
  after popover reassign / timeline reassign / edit / split; same-target
  suggestions become the offer; apply = user's word via
  reassignTimelineSessions with re-offer suppression; deselected stay
  suggestions; refileMode off silent). TimelineView pass mode: banner →
  dim irrelevant slices, dashed outlines on candidates (preselected),
  click toggles, approve bar. Manual synced.
- **Deliberate v1 cuts (in TODO):** click-DRAG range toggle; auto-widen
  the visible window to the earliest candidate.
- **Checks:** Mac 955/0 + snapshots build (UI gate). Commit ad5be7c,
  pushed, CI green.
- **Outcome:** success. ALL SEVEN optimiser items now landed in some
  form. Remaining tails: end-time-orphan auto-apply, spec batch 5 redo
  audit, propagation polish, ledger reverse-index design.
- **Notes:** fromMartin polled at iter 15: empty.
## Iter 17 — end-time-orphan auto-apply + ledger reverse index (RECONSTRUCTED)

- **Provenance note:** this block was written at resumption 2026-08-13
  23:55Z — the session died ~21:01Z after both commits pushed but before
  the block landed. Reconstructed from the commits + CHANGELOG; treat the
  narrative as derived, the SHAs as ground truth.
- **Built (5ee2b51):** end-time-orphan auto-apply — trimming an entry's
  start/end auto-fills vacated stretches recorded windows still cover:
  AwayRescue.plan orphan mode (requireAwayMarked: false) at engine
  certainty/provenance (red + queued below bar), interval-subtracts
  neighbour coverage, never teaches (L2), same ⌘Z as the edit. +1 check.
  Linux 810/0, Mac 956/0.
- **Built (e49e248):** CorrectionImpact reverse index — click a
  Corrections row → what that correction has since decided (entries,
  total time, honest same-app-pull flag; computed at view time, no schema
  change). PLUS the structural UI-gate fix: timeandeyeChecks gains
  timeandeyeUI as a macOS-conditional dependency — the suite now compiles
  the whole SwiftUI layer every run. +2 checks. Linux 812/0, Mac 958/0.
- **Outcome:** success (verified post-hoc: CI green on both — runs
  31743329583, 31743686327).
- **Notes:** the UI-gate fix supersedes fromClaude item 8 (manual CI
  step) — retracted at resumption.

## Resumption — 2026-08-13T23:55:25Z

- Halt cause: session killed ~21:01Z (after e49e248 push, before its iter
  block); marker found active=0 remaining=2 — launcher window ended.
- New invocation: `/vsss --sessions 5 --resume` → marker re-armed
  active=1 remaining=4; fresh 5h window budget from 23:55:25Z per
  --sessions budget arithmetic (window START_TIME epoch 1786665325).
- git log verified against iter blocks: two commits beyond iter 16, both
  reconstructed above as iter 17; CI green through e49e248.
- fromMartin polled: empty (all 8 prior items still live bar the
  retraction). fromClaude item 8 retracted as obsolete, archived to Q&A
  archive; items 1-7 unchanged, next thread number stays 9.
- Continuing at iter 18; optimiser dispatched for next-work order.
- Baselines re-verified at HEAD (e49e248) at resumption: Mac bridge
  958/0, Linux subset 812/0 — both suites run clean before any new work.
- **Optimiser (post-iter-17, opus) ranked order:** (1) tab-URL/AX read
  off the main run loop (Sensors.swift — iter 6 RAISED the sync-call
  rate; EmailCaptureEngine shape); (2) observedWhileAway filters at
  windowBoundaries/reassignSpentApp/TimelineView zoom strip; (3) calendar
  candidate ranking (timed + primary first); (4) B7 URL surface-key
  query/fragment grain; (5) media-playback presence (no idle-out);
  (6) menu-bar vs timeline elapsed semantics; (7) undo/redo batch 5;
  (8) flush save-before-clear; (9) light-scheme highlight + dual-
  appearance snapshots; (10) propagation polish (drag toggle +
  auto-widen). Verified findings: sync NSAppleScript still at
  Sensors.swift activeTabURL; away filter exists in exactly one place;
  AppController is 6,421 lines (TODO's "1,9xx" stale).
## Iter 18 — sensor poll non-blocking (optimiser rank 1)

- **Plan:** kill the last sync AppleScript on the sensor hot path.
  TabURLEngine (EmailCaptureEngine discipline: osascript subprocess,
  deadline 1.5 s, one-in-flight, main-confined state) + browser+title
  URL cache; poll() reads cache instantly, refresh corrects off-main and
  re-polls (settle guard vs redundant fetches; 10 s slow cadence catches
  SPA same-title URL changes). AX reads (title + observer re-homing) get
  1 s AXUIElementSetMessagingTimeout. runOsascript de-privatised for
  reuse. Known self-healing staleness (title-shared tabs, switch-during-
  fetch) documented in the engine header.
- **Files:** TabURLEngine.swift (new), Sensors.swift,
  EmailCaptureEngine.swift, TabURLChecks.swift (new, registered),
  CHANGELOG.md, TODO.md (parked 2026-07-01 sensor item closed).
- **Checks:** +8 TabURL (policy truth table, cache change/eviction,
  script sources). Mac bridge 966/0 (suite compiles UI). Linux subset
  untouched by construction (timeandeyeMac not built there).
- **Commit:** 8c821c6, pushed; CI watch armed (15-min timer).
- **Outcome:** success. On-device soak rides Martin's existing latency
  feel-test (fromClaude 7); no new ask needed.
- **Notes:** fromMartin polled at resumption: empty.

## Iter 19 — away shadow-span filters (optimiser rank 2)

- **Plan:** the three Mac-side readers the 2026-08-07 away item named:
  windowBoundaries (snap edges), reassignSpentApp (carve ranges),
  timelineSpans (window-detail strip + span-reassign bar → also closes
  teach-from-shadow-rows). Site audit confirmed every OTHER spans reader
  is either choke-point-defended (FocusSpan.dominant), deliberately
  away-reading (rescue + orphan mode), or aggregator-covered.
- **Files:** AppController.swift (3 one-line filters + comments),
  TODO.md (away item + 13 Aug away entry closed [x]), CHANGELOG.md.
- **Checks:** none added — the checkable property (shadow rows never win
  identity/teach/aggregate) is already pinned at the FocusSpan.dominant
  choke point; these are Mac-side query filters with no pure seam.
  Manual already consistent ("never billed, never taught"; rescue is the
  viewer). Mac bridge 966/0.
- **Commit:** 4dd2b05, pushed; CI (concurrency group supersedes 8c821c6's
  run; both watched by the armed timer).
- **Outcome:** success. Known consequence recorded in TODO: rescued/
  pinned stretches show no window panes — revisit only if it annoys.
- **Notes:** fromMartin polled at iter start: empty.

## Iter 20 — calendar candidate ranking (optimiser rank 3)

- **Plan:** both consumers picked events in EventKit fetch order —
  calendarHint (.first(where: overlaps)) and recomputeCalendarMatch
  (first live event with a rule). New Core CalendarSelection: span form
  (timed > 1-day all-day > multi-day; primary; overlap; shorter;
  stable) + point form (primary; shortest covering; latest start).
  CalendarEvent gains primary (bridge maps EventKit
  defaultCalendarForNewEvents — best available proxy). Nothing excluded
  (spec §7 all-day hint stays legitimate when alone).
- **Files:** CalendarMatch.swift, CalendarBridge.swift,
  AppController.swift (2 call sites), CalendarMatchChecks.swift (+6,
  registered as CalendarSelection), main.swift, CHANGELOG, TODO,
  MANUAL.md + site manual (one-sentence sync in both).
- **Checks:** Linux 818/0, Mac 972/0.
- **Commit:** 9f5bb2b, pushed; CI supersedes previous run, timer watch
  still armed.
- **Outcome:** success.
- **Notes:** fromMartin polled at iter start: empty. Optimiser rank 4
  (B7 URL grain) DEFERRED until fromClaude 11 (a/b) answered — its
  persisted-key migration shape depends on the answer; ranks 5-7 are
  ask-independent, taken next.

## Iter 21 — media playback = presence (optimiser rank 5, audio half)

- **Plan:** SensorEvent.mediaPlayback; SessionTracker holds off the idle
  check while playback runs, bumps lastInput to the playback END on the
  false transition (idle counts from silence; retro-trim lands there);
  truthful through Away. Mac: MicMonitor generalised to
  AudioActivityMonitor(deviceSelector:) — two instances (input=mic,
  output=playback), transitions DebugLogged (an always-running output
  device would silently kill idle-stop — findable). Generalisation
  design question (muted video/reads/remote desktop) stays with Martin.
- **Files:** Models.swift, SessionTracker.swift, Sensors.swift,
  SessionTrackerChecks.swift (+1, both halves), CHANGELOG, TODO,
  MANUAL.md + site popover page.
- **Checks:** Linux 819/0, Mac 973/0. **Commit:** 980dbff, pushed; CI in
  progress (timer armed), 4dd2b05 + 9f5bb2b confirmed green.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty. fromClaude item 9 REWRITTEN
  (same thread): now records BUILT status + the background-music flip
  side; asks acceptable-as-built + generalisation scope in one.

## Iter 22 — elapsed-desync audit + pins (optimiser rank 6)

- **Audit finding:** the under-count Martin saw (menu < timeline) is
  structurally impossible since the ownsSlice gate + max(slice,
  banked+running) landed: menu and timeline live block share
  liveSliceStart. The over-count direction (menu > block via banked
  earlier visits) is DESIGNED continuity — kept and recorded.
- **Built:** 2 pinning asserts in the scoped-relabel check (owner + slice
  start at the cut). TODO item closed with the decision; fromClaude 10
  rewritten from "being built" to "verified + the deliberate remainder".
- **Checks:** Linux 819/0, Mac 973/0. **Commit:** a66c803, pushed.
- **Outcome:** success (audit + pins; no behaviour change).
- **Notes:** fromMartin polled: empty.

## Iter 23 — undo/redo batch 5 (optimiser rank 7)

- **Audit:** assignReview (id-keyed assign + re-points, teaches
  recompute), applyTimelineEdit (id-keyed update; sever/orphan-fill
  re-run), reassignTimelineSessions (per-id sever + update) — all
  id-stable → explicit redo closures added (iter-12/13 idiom).
  replaceSession (fresh piece ids) + whole undoGroup entries stay
  boundaries per the spec's own rule; group-member re-registration
  deliberately not attempted. Spec carries the batch-status audit.
- **Files:** AppController.swift (3 sites), spec, TODO, CHANGELOG.
- **Checks:** Mac 973/0 (registration glue; UndoStack routing already
  check-pinned). **Commit:** 10579f9, pushed.
- **Outcome:** success — the undo-redo spec is now fully landed.
- **Notes:** fromMartin polled: empty.

## Iter 24 — save-before-clear (optimiser rank 8)

- **Built:** Mac — failed flush saves re-stage in unsavedSessions +
  unsaved-sessions.json sidecar (different failure domain), retried at
  startUp / every flush / every sync kick. Phone — stop() reordered so
  the crash checkpoint is deleted only after the save lands; failures
  hold the slice (lifecycle-tick retry) with the checkpoint as the
  durable copy; init gains journal injection for checks. Bug found en
  route: InMemory mock's update dropped missing-row writes where SQLite
  upserts — the phone checkpoint silently never checkpointed on the
  mock; aligned.
- **Checks:** +1 phone (fail→hold→retry→release, injected failing
  store). Linux 820/0, Mac 974/0. **Commit:** 1c7e3c4, pushed. CI green
  through 10579f9.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty. Optimiser ranks 1-3 and 5-8 now
  done; rank 4 (B7 grain) awaits fromClaude 11; remaining: rank 9
  (light-mode highlight + dual-appearance snapshots), rank 10
  (propagation polish), then below-the-line items.

## Iter 25 — light-mode highlight + dual-appearance snapshots (rank 9)

- **Built:** AndeyeTheme.Colours.highlight appearance-resolved via
  canImport-guarded NSColor/UIColor dynamic providers (dark keeps
  0.45/0.78/1.0; light gets 0/0.35/0.72); theme header contract reworded
  (SwiftUI-first, guarded platform-colour construction only).
  SnapshotHarness: snap() → two renders per view with explicit
  NSAppearance (-light/-dark). VERIFIED BY EYE: popover renders pulled
  over the bridge — genuine white/dark backgrounds respectively.
- **Checks:** Mac 974/0 (ThemeChecks equality unaffected); snapshots run
  = the render gate. **Commit:** 10f1195, pushed.
- **Outcome:** success. Final light value awaits Martin's eye (TODO
  notes it).
- **Notes:** fromMartin polled: empty.

## Iter 26 — propagation drag-sweep + auto-widen (rank 10; reply 8 done)

- **Built:** drawGesture pass-mode branch paints swept candidates to one
  value (fixed by first candidate touched; propagationPaint state);
  draw/range drags inert during the pass; "Review them here" auto-widens
  viewStart to the earliest candidate (AppController.earliestSessionStart
  — findings carry only ids), right edge pinned, clampViewport bounds.
  Banner copy + both manuals updated.
- **Checks:** Mac 974/0 (suite compiles UI). **Commit:** d465475,
  pushed; CI green through 10f1195.
- **Outcome:** success. Optimiser tranche 1 fully consumed (rank 4
  gated on fromClaude 11). Fresh optimiser dispatched for tranche 2.
- **Notes:** fromMartin polled: empty.

- **Optimiser tranche 2 (post-iter-26, opus; all candidates verified
  against HEAD):** (1) away-END rescue offer (setAway(false) + auto-clear
  sites → banner/notification into Settings ▸ Away rescue, should-offer
  rule in Core); (2) silent-data-loss visibility (unsavedSessions count +
  decode-drop counters → Maintenance panel; closes D0.3); (3) popover
  mode label = vary path + right-click default; (4) SettingsIA index
  gaps (away rescue/billable/corrections/undo) + Xero tier-heading
  mislabel; (5) positive fullscreen detector (TODO's NSPanel item found
  STALE — SpaceJoiningView already does it; auto-hide users are the real
  remainder); (6) Electron per-document surface keys (TabURLEngine
  discipline + AXManualAccessibility, per-bundle allowlist);
  (7) [+similar]/[-similar] ladder; (8) email-recipe health surface in
  Settings; (9) AppController extraction (file is 6,499 lines, TODO says
  1,9xx — fix claim); (10) comment-lifecycle edges. Not ranked: invoice
  NUMBER (Pro-repo-only call), legal redirects (gated), workspace
  layouts (abandonment holds), D2(b)/(c) (CloudKit GA).
- **fromClaude generation update (pre-iter-27):** appended 12 (Electron
  chat-title one-liner), 13 (onboarding scope), 14 (adjacency fitting
  route), 15 (andeye.com terms/privacy live?). Next number 16.

## Iter 27 — away-end rescue offer (tranche-2 rank 1)

- **Built:** AwayRescue.shouldOffer (5-min evidence floor, Core) +
  offerAwayRescueIfDue wired at all three Away ends (setAway(false),
  guard-path clear, userStopped; scheduled stop routes via setAway).
  Popover banner (calendar-mismatch idiom) with Rebuild…/dismiss;
  focusAwayRescue stages pendingRescueFocus; SettingsView consumes it
  (jump to Maintenance, auto-scan, overlap-matched auto-preview).
  Notification via notifyContent. Manual: 3 pages synced.
- **Checks:** +1 (offer floor). Linux 821/0, Mac 975/0. **Commit:**
  eb2b4dc, pushed.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty. Offer threshold default (5 min)
  recorded in CHANGELOG/manual; Martin can amend on feel.

## Iter 28 — silent-loss visibility (tranche-2 rank 2; D0.3 store half)

- **Built:** SQLiteJournalStore.decodeRow chokepoint (14 sites swapped;
  distinct-payload hash set, lock-correct) + JournalStore.
  undecodableRowsObserved (protocol + default 0); Maintenance journal
  summary gains "N held after a failed save (retrying)" + "N from a
  newer Time&I version (not visible here)"; failure path updates the
  summary immediately. CloudKit pull-side counter recorded in TODO as
  the rides-with-sync-GA remainder.
- **Checks:** Linux 821/0, Mac 975/0 (advisory counter — no corrupt-row
  fixture writable through the store API; mechanism is one guarded
  chokepoint). **Commit:** a0e8ea1, pushed; CI green through eb2b4dc.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty.

## Iter 29 — popover mode label vary-click + right-click default (rank 3)

- **Built:** the Reassign/Switch-to caption is a plain-styled Button
  (toggles changeMode; snap-back untouched) with a contextMenu writing
  settings.popoverDefaultsToChangeMode (✓ on current). Manual both
  pages; TODO 2026-07-17 item closed.
- **Checks:** Mac 975/0; popover snapshot re-rendered + eyeballed —
  layout unchanged. **Commit:** fa88ca3, pushed.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty.

## Iter 30 — Settings search currency + tier headings (rank 4)

- **Built:** SettingsIA items for Away rescue / Billable items /
  Corrections ledger (+1 check pinning landing categories; undo/redo has
  no Settings control, deliberately not indexed). "Pro connectors" →
  "Standard connections" + "Premium connections" (locked-tier
  vocabulary; registry-driven rendering stays in TODO). Manual fixed
  incl. a STALE claim the removed Xero upgrade link still existed.
- **Checks:** Linux 822/0, Mac 976/0. **Commit:** 31dbed8, pushed.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty.

## Iter 31 — fullscreen items audit (rank 5, deliberately not shipped)

- **Finding:** the NSPanel TODO item was STALE (SpaceJoiningView already
  applies the full pose; closed [x]). The auto-hide positive detector
  was designed then rejected against its own provable-or-don't bar:
  exact-cover CGWindowList matching false-positives on maximised windows
  with a hidden Dock → permanent squatting, worse than the current
  degrade. Promising discriminator (Dock-owned fullscreen backdrop
  window) needs an on-device CGWindowList dump — recorded in the TODO
  item as the next step when Martin wants it.
- **Commit:** 69ecae0 (docs only), pushed.
- **Outcome:** audit + honest no-ship. Rank 6 (Electron keys) DEFERRED
  pending fromClaude 12 — the mechanism depends on whether Claude
  desktop's window names already differ per chat.

## Iter 32 — [+similar]/[−similar] ladder (rank 7)

- **Built:** Core SpanSimilarity (3-rung ladder, Comparable levels) +
  stateless widen/narrow closures in TimelineView's card construction
  (widen = first rung not fully selected; narrow = highest fully-selected
  rung minus the rung below; focused span invariantly selected);
  EvidenceCardView buttons beside + all. Manual both pages.
- **Checks:** +1 Models (ladder + superset property). Linux 823/0,
  Mac 977/0. **Commit:** 2ef1946, pushed.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty.

## Iter 33 — email-recipe health surface (rank 8)

- **Built:** EmailSystem.userLabel (Core copy names);
  AppController.unhealthyEmailSystems(); Settings ▸ Email orange notice
  (quiet while healthy) + SettingsIA "Email matching health" item.
  Self-heal loop stays parked per the optimiser. Manual both pages.
- **Checks:** Linux 823/0, Mac 977/0. **Commit:** ef2f0cf, pushed.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty.

## Iter 34 — dropped-flit comment banking (rank 10)

- **Built:** onState stop branch banks leftover manualNotes onto the
  most recent slice journalled within the hour (joinedNote fold; still
  editable) before removeAll; no slice → drop as before, documented why.
- **Checks:** Mac 977/0 (Mac-side closure; no pure seam — mechanism is
  8 lines behind the existing flush-consume). **Commit:** 6804e57,
  pushed.
- **Outcome:** success. Tranche 2 consumed: ranks 1-5,7,8,10 done
  (5 = honest no-ship analysis), 6 gated on fromClaude 12, 9 (extraction)
  parked as zero-user-value until sweeps run dry. Tranche-3 optimiser
  dispatched.
- **Notes:** fromMartin polled: empty.

- **Optimiser tranche 4 (post-iter-34, opus; 3 parallel audits, claims
  spot-verified, Linux 823/0 re-confirmed at HEAD):** (1) OP permanent-
  error classification (401/403/404 burn to .stuck forever; 422 amend
  fallback missing; "N waiting" surface); (2) Accessibility/Automation
  loss = total silent degrade, no surface, not even in the manual;
  (3) hot-path hardening (3 AX observer calls without messaging
  timeouts; UndoStack unbounded incl. LearningStore snapshots; sync
  verify walks unbounded; TimelineView O(N²) card scans); (4) timesheet
  export wrong for invoicing (row/total rounding mismatch, CSV formula
  injection, failure→empty "Copied"); (5) stale popover offers
  (previousTask never cleared on stop; away re-offer unsuppressed);
  (6) failed local write still PATCHes remote (+ refileDismissals
  no-undo, AI-paste wiped on parse failure); (7) keyboard-path debt for
  this session's affordances (propagation pass Esc actively refused);
  (8) Settings-search lies (phantom "Corrections ledger" item pinned
  green by its own check!) + manual sells hard-cap delete on a false
  CloudKit premise. Extraction stays fallback. Defaults adopted without
  asking: banner reword (no click targets), refileDismissals gets
  inspect/clear built, forget-all keeps one-click + ⌘Z (his reply-4
  spec), rounding = round-half-up rows with totals summing rounded rows
  (FYI-unless-object posted).
- **fromClaude generation update (pre-iter-35):** appended 16 (iOS
  slice-repair UI spend — unverifiable without Xcode), 17 (timesheet
  rounding FYI-unless-object). Next number 18.

## Iter 35 — OP failure classification + stuck surface (tranche-4 rank 1)

- **Arbitration note:** the tranche-3 optimiser emitted TWO reports
  (its sub-agents timed out; the second dropped two items as "sound").
  I verified the disputed claims myself: OPBackend genuinely never threw
  PermanentPostError (report 1 right — the ENGINE machinery being sound
  is what report 2 checked); timesheet export claims still need
  first-hand verification before that item is touched. Merged ranking
  recorded; proceeding on my own verification, not either report alone.
- **Built:** BackendAuthError (Core); OPBackend classifyPost (401/403
  auth, 404/422 permanent, 5xx transient) + classifyAuth on amendments +
  422 drop-startTime fallback for UPDATE (parity with create);
  SyncEngine auth catch (attempts unchanged, .failed, pass ends, named
  error); AppController stuckPostingCount (@Published, refreshed at
  startUp / per sync pass / on Retry) + popover "N entries can't post" +
  Fix banner. Manual both.
- **Checks:** +2 (engine: five auth passes burn zero attempts then post
  on heal; OPBackend status classification incl. double-422 fallback
  proof via MockTransport request count). Linux 825/0, Mac 979/0.
- **Commit:** 8b2bb59, pushed; CI green through 6804e57.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty.

## Iter 36 — sensing-health surface (tranche-4 rank 2)

- **Built:** TabURLEngine.automationDenied (set on -1743, self-clears on
  a good read); SensorHub pass-through; AppController
  accessibilityMissing/automationBlocked + openPrivacyPane deep-links;
  popover orange banner (accessibility takes priority) + Settings ▸
  Tracking "Sensing permissions" conditional section (per-problem copy +
  pane buttons); SettingsIA item; manual gains its FIRST permissions
  passage (both files).
- **Checks:** Linux 825/0, Mac 979/0; snapshots render clean (the
  headless account's own missing grants exercise the notice path).
- **Commit:** e3fe442, pushed.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty.

## Iter 37 — stale-offer guards (tranche-4 rank: previousTask + away
   re-offer, both optimiser reports agreed)

- **Built:** previousTask cleared in the stop branch; revertTargetTask
  gains a 15-min freshness gate on taskChangedAt. Away offers:
  offeredAwayStretchEnds one-shot set + rebuiltAwayRanges (recorded at
  applyAwayRescue over the plan's span; in-memory by design — ⌘Z/relaunch
  keep the deliberate Settings path); isStretchRebuilt drives both
  suppression and the Settings "rebuilt ✓" tag.
- **Checks:** Mac 979/0 (Mac-side glue; no pure seam). **Commit:**
  6d05f84, pushed.
- **Outcome:** success.
- **Notes:** fromMartin polled: empty.

- **fromClaude generation update (pre-iter-18):** items 1-7 unchanged;
  appended 9 (media-presence design ask), 10 (menu-clock semantics
  FYI-unless-object: menu matches timeline live slice), 11 (URL grain
  a/b). Next number 12. Questions posted, work proceeds on defaults —
  nothing blocks.
- **LIVE DIRECTIVE (Martin, terminal, 2026-08-13 ~23:57Z):** "When you
  reach the perfection gate, just keep going, figure out whatever else
  you can perfect in the app and perfect it. Don't stop until I
  specifically ask you to stop." — Exit condition 2 (optimiser
  perfection gate) and condition 4 (three no-op iterations) are
  SUSPENDED for this run: an optimiser "stop" verdict converts to a
  fresh find-work sweep. Conditions 1 (hard-escalate), 3 (window credit
  exhaustion — --sessions relaunch handles it), and 5 (destructive git
  state) still stand, as does the full safety floor.

## Iter 38 — hot-path hardening + honoured pin (tranche-4 rank 3, final iter)

- **Built:** AX observer attach gains the 1 s messaging timeout (the last
  unbounded cross-process AX call, fires per app activation);
  similarityIndex memo — one grouped pass per render serves every card's
  + all / + similar sets (was ~4·N² after ⌘A). Undo-stack depth cap
  ATTEMPTED then REVERTED: it tripped the pinned "infinitely undoable —
  no silent depth limit" check (2026-07-09 audit) — the pin is Martin's
  recorded call, so the memory trade-off went to him as fromClaude 18
  (a: cap 500 / b: diff-based snapshots / c: leave) instead of being
  overridden autonomously.
- **Checks:** Linux 825/0, Mac 979/0. **Commit:** dc3b21b, pushed; CI
  GREEN confirmed (watched to conclusion).
- **Outcome:** success.
- **Notes:** Martin's live terminal instruction mid-iter: "OK finish at
  the end of the current tasks please" — supreme precedence; the loop
  exits cleanly after this iteration.

## Final state

- **Exit reason:** user-requested stop (live terminal instruction,
  supersedes the earlier suspend-the-perfection-gate directive). No
  hard-escalate, no destructive state; credit remained.
- **End time:** 2026-08-14T01:55Z (window 2 ran 23:55Z→01:55Z of its 5h).
- **Iterations:** 38 total (1-17 window 1; 18-38 this window, including
  two audit/no-ship iterations recorded honestly: fullscreen detector,
  undo-cap revert).
- **Commits this window (21, all pushed, all CI-green through dc3b21b):**
  8c821c6 sensor poll non-blocking; 4dd2b05 away shadow filters;
  9f5bb2b calendar ranking; 980dbff media presence; a66c803 elapsed
  pins; 10579f9 undo/redo batch 5; 1c7e3c4 save-before-clear;
  10f1195 light-mode highlight + dual snapshots; d465475 propagation
  drag-sweep/auto-widen; eb2b4dc away-end rescue offer; a0e8ea1 silent-
  loss visibility; fa88ca3 popover mode label; 31dbed8 SettingsIA +
  tier headings; 69ecae0 fullscreen audit (docs); 2ef1946 +similar;
  ef2f0cf email health; 6804e57 flit-comment banking; 8b2bb59 OP
  failure classification; e3fe442 sensing-permission surface;
  6d05f84 stale-offer guards; dc3b21b AX timeout + card memo.
- **Pushed:** yes — pre-authorised by the original invocation args;
  CI watched to green throughout (concurrency supersession accounted).
- **Escalations:** none. One pinned-decision collision (undo cap)
  resolved by reverting and asking, not overriding.
- **Deferred (all gated on fromClaude answers or environment):** B7 URL
  grain (11 a/b); Electron per-chat keys (12); presence generalisation
  (9); onboarding scope (13); adjacency fitting route (14); legal
  redirects (15); iOS slice-repair UI (16); undo memory policy (18);
  positive fullscreen detector (needs on-device CGWindowList dump);
  invoice NUMBER (Pro repo); D2(b)/(c) (CloudKit GA); AppController
  extraction + remaining tranche-4 tails (timesheet export
  verification, keyboard-path debt, ledger truncation notice,
  failed-write-then-PATCH ordering, refileDismissals surface, phone
  engine gaps) — all recorded in the tranche-4 arbitration note above
  for the next session's optimiser.
